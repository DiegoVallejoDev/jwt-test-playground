export const config = Object.freeze({
    secret: "secret61789das8duauca9", // should be imported from .env file but this is a toy project
    SESSION_TIMEOUT_MINUTES: 2,
    REFRESH_TIMEOUT_MINUTES: 10,
})